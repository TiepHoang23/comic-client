export enum ComicStatus {
  ALL = -1,
  ONGOING = 0,
  COMPLETED = 1,
}

export enum SortType {
  Chapter,
  LastUpdate,
  TopFollow,
  TopComment,
  NewComic,
  TopDay,
  TopWeek,
  TopMonth,
  TopAll,
}

export enum TopType {
  Day = 1,
  Week = 2,
  Month = 3,
}
